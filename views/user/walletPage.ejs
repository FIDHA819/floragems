<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Wallet Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f4f7f6;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
            padding-bottom: 80px;
        }

        header, footer {
            background-color: #343a40;
            color: white;
            padding: 15px 0;
        }

        .breadcrumb {
            background-color: transparent;
            padding: 0;
            font-size: 14px;
        }

        .breadcrumb a {
            color: #6c757d;
            text-decoration: none;
        }

        .breadcrumb a:hover {
            color: #28a745;
        }

        .dashboard-menu {
            background-color: #ffffff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .dashboard-menu ul {
            padding-left: 0;
            list-style: none;
        }

        .dashboard-menu .nav-item {
            margin-bottom: 15px;
        }

        .dashboard-menu .nav-link {
            color: #343a40;
            font-weight: bold;
            text-transform: uppercase;
            padding: 10px 15px;
            border-radius: 5px;
            transition: background-color 0.3s;
        }

        .dashboard-menu .nav-link:hover,
        .dashboard-menu .nav-link.active {
            background-color: #cf876ba8;
            color: white;
        }

        .main-content {
            background-color: white;
            border-radius: 8px;
            padding: 20px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .card-header {
            background-color: #cf876ba8;
            color: white;
            text-align: center;
            font-weight: bold;
            border-radius: 8px 8px 0 0;
        }

        .card-body {
            padding: 20px;
            background-color: #ffffff;
            border-radius: 0 0 8px 8px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        .btn-primary {
            background-color: #28a745;
            color: white;
            border-radius: 5px;
            padding: 8px 15px;
            font-weight: bold;
        }

        .btn-primary:hover {
            background-color: #218838;
        }

        .btn-sm {
            padding: 6px 12px;
            font-size: 12px;
        }

        .transaction-table th,
        .transaction-table td {
            text-align: center;
        }

        footer {
            margin-top: auto;
            padding: 15px 0;
            text-align: center;
        }
        /* General styles for the main container */
.main {
    font-family: 'Arial', sans-serif;
    background-color: #f9f9f9;
    padding: 20px 0;
}

/* Styles for the breadcrumb wrap */
.page-header.breadcrumb-wrap {
    background-color: #fff;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    padding: 20px 0;
    border-radius: 8px;
}

/* Breadcrumb container */
.breadcrumb-wrap .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 15px;
}

/* Breadcrumb text styling */
.breadcrumb {
    font-size: 16px;
    color: #6c757d;
    display: flex;
    align-items: center;
    gap: 5px;
}

/* Breadcrumb links */
.breadcrumb a {
    text-decoration: none;
    color: #007bff;
    font-weight: 500;
    transition: color 0.3s ease;
}

.breadcrumb a:hover {
    color: #0056b3;
}

/* Separator between links */
.breadcrumb span {
    color: #6c757d;
    font-size: 14px;
}

/* Mobile responsiveness */
@media (max-width: 768px) {
    .breadcrumb {
        font-size: 14px;
    }
}

    </style>
</head>
<body>
    <header>
        <div class="container text-center">
            <h2>Wallet Dashboard</h2>
        </div>
    </header>

    <main class="main">
        <div class="page-header breadcrumb-wrap mb-3">
            <div class="container">
                <div class="breadcrumb">
                    <a href="/" rel="nofollow">Home</a>
                    <span></span> Profile <span></span> Wallet
                </div>
            </div>
        </div>

        <section class="pt-10 pb-10">
            <div class="container">
                <div class="row">
                    <div class="col-lg-10 m-auto">
                        <div class="row">
                            <!-- Sidebar -->
                            <div class="col-md-4">
                                <div class="dashboard-menu">
                                    <ul class="nav flex-column" role="tablist">
                                        <li class="nav-item">
                                            <a class="nav-link active" href="#dashboard" role="tab">Dashboard</a>
                                        </li>
                                        <li class="nav-item">
                                            <a class="nav-link" data-bs-toggle="pill" href="#address" role="tab">My Address</a>
                                        </li>
                                        <li class="nav-item">
                                            <a class="nav-link" href="/myOrders" role="tab">Orders</a>
                                        </li>
                                        <li class="nav-item">
                                            <a class="nav-link" href="/wallet" role="tab">wallet</a>
                                        </li>
                                        <li class="nav-item">
                                            <a class="nav-link" href="/Referral" role="tab">Referral</a>
                                        </li>
                                        <li class="nav-item">
                                            <a class="nav-link" href="/logout">Logout</a>
                                        </li>
                                    </ul>
                                </div>
                            </div>

                            <!-- Wallet Content -->
                            <div class="col-md-8">
                                <div class="tab-content dashboard-content">
                                    <!-- Wallet Overview -->
                                    <div class="tab-pane fade show active" id="wallet" role="tabpanel">
                                        <div class="card">
                                            <div class="card-header">Wallet Overview</div>
                                            <div class="card-body">
                                                <h5>Wallet Balance: ₹<%= walletBalance %></h5>
                                                <div class="mt-3">
                                                    <a href="/addFunds" class="btn btn-primary btn-sm">Add Funds</a>
                                                    <a href="/withdrawFunds" class="btn btn-primary btn-sm">Withdraw Funds</a>
                                                </div>
                                            </div>
                                        </div>

                                        <!-- Transaction History -->
                                        <div class="card mt-4">
                                            <div class="card-header">Transaction History</div>
                                            <div class="card-body">
                                                <table class="table transaction-table">
                                                    <thead>
                                                        <tr>
                                                          <th>Date</th>
                                                          <th>Description</th>
                                                          <th>Type</th>
                                                          <th>Amount</th>
                                                          <th>Status</th>
                                                        </tr>
                                                      </thead>
                                                      <tbody>
                                                        <% if (transactions.length > 0) { %>
                                                          <% transactions.forEach((transaction) => { %>
                                                            <tr>
                                                              <td><%= transaction.createdAt.toLocaleDateString() %></td> <!-- Format the date -->
                                                              <td><%= transaction.description %></td>
                                                              <td><%= transaction.type %></td>
                                                              <td>
                                                                <% if (transaction.type === 'Credit') { %>
                                                                  ₹<%= transaction.amount %> <!-- Display credited amount -->
                                                                <% } else if (transaction.type === 'Debit') { %>
                                                                  -₹<%= transaction.amount %> <!-- Display debited amount -->
                                                                <% } %>
                                                              </td>
                                                              <td><%= transaction.status %></td>
                                                            </tr>
                                                          <% }); %>
                                                        <% } else { %>
                                                          <tr>
                                                            <td colspan="4">No transactions found.</td>
                                                          </tr>
                                                        <% } %>
                                                      </tbody>
                                                      
                                                </table>
                                            </div>
                                        </div>
                                    </div>
                                    <!-- End Wallet Overview -->
                                </div>
                            </div>
                            <!-- End Content Area -->
                        </div>
                    </div>
                </div>
            </div>
        </section>
    </main>

    <footer>
        <p>&copy; 2025 Your Company. All Rights Reserved.</p>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
